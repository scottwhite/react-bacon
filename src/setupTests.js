import trans from './i18n';
import fs from 'fs';
import path from 'path';

// let fp = path.resolve(__dirname, './locales/en/strings.json');
// let phrases = JSON.parse(fs.readFileSync(fp));
trans.setup('en');
